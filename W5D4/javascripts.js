function madLib(verb, adj, noun) {
  return `We shall ${verb} the ${adj.toUpperCase()} ${noun.toUpperCase()}.`
}

function madLib(verb, adj, noun) {
  return `We shall ${verb} the ${adj.toUpperCase()} ${noun.toUpperCase()}.`
}


function madLib(verb, adj, noun) {
  return `We shall ${verb} the ${adj.toUpperCase()} ${noun.toUpperCase()}.`
}
